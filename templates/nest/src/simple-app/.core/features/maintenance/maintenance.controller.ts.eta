/**
 * This file was automatically generated by simpleapp generator. Every
 * MODIFICATION OVERRIDE BY GENERATEOR
 * last change 2025-09-01
 * Author: Ks Tan
 */
import { Controller, Get, Post, Param, Logger, HttpCode } from '@nestjs/common';
import { MaintenanceService } from './maintenance.service';
import { ApiTags, ApiBody, ApiResponse, ApiOperation, ApiQuery } from '@nestjs/swagger';
import { Roles } from 'src/simple-app/.core/commons/roles/roles.decorator';
import { Role } from 'src/simple-app/.core/commons/roles/roles.enum';
import * as schemas from './schemas';
import { UserContext } from 'src/simple-app/.core/commons/user.context';
import { AppUser } from 'src/simple-app/.core/commons/decorators/appuser.decorator';
@ApiTags('maintenance')
@Controller('maintenance')
@Roles(Role.SuperAdmin)
export class MaintenanceController {
  private logger = new Logger();
  constructor(private readonly maintenanceSvc: MaintenanceService) {}

  @Get('/get-updates')
  @Roles(Role.SuperAdmin)
  @ApiResponse({
    status: 200,
    description: 'Sample 200 response',
    type: [schemas.UpgradeScript],
  })
  async getUpdate(@AppUser() appuser: UserContext) {
    const result = await this.maintenanceSvc.getUpdates(appuser);
    return result;
  }

  @Post('/run-updates/:id')
  @HttpCode(200)
  @Roles(Role.SuperAdmin)
  @ApiResponse({
    status: 200,
    description: 'Sample 200 response',
    type: [String],
  })
  async runUpdate(@AppUser() appuser: UserContext, @Param('id') id: string) {
    const result: string = await this.maintenanceSvc.runUpdates(appuser, id);
    return result;
  }

  @Get('/service-status')
  @Roles(Role.SuperAdmin)
  @ApiResponse({
    status: 200,
    description: 'Sample 200 response',
    type: Boolean,
  })
  async runGetServiceStatus(@AppUser() appuser: UserContext) {
    const result: Boolean = await this.maintenanceSvc.runGetServiceStatus(appuser);
    return result;
  }

  @Post('/stop-service')
  @HttpCode(200)
  @Roles(Role.SuperAdmin)
  @ApiResponse({
    status: 200,
    description: 'Sample 200 response',
    type: String,
  })
  async runStopService(@AppUser() appuser: UserContext) {
    const result: string = await this.maintenanceSvc.runStopService(appuser);
    return result;
  }

  @Post('/start-service')
  @HttpCode(200)
  @Roles(Role.SuperAdmin)
  @ApiResponse({
    status: 200,
    description: 'Sample 200 response',
    type: String,
  })
  async runStartService(@AppUser() appuser: UserContext) {
    const result: string = await this.maintenanceSvc.runStartService(appuser);
    return result;
  }
}
