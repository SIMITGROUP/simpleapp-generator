import { UserContext } from "../../user-context/user.context"
import { DeletePayload } from "../event.type"
export interface EventBeforeDelete<T> {
    beforeDelete(appUser:UserContext, payload: DeletePayload<T> ): Promise<void>
}