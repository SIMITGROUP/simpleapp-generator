import { UserContext } from "../../user-context/user.context"
import { UpdatePayload } from "../event.type"
export interface EventAfterUpdate<T> {
    afterUpdate(appUser:UserContext,payload: UpdatePayload<T> ): Promise<void>
}