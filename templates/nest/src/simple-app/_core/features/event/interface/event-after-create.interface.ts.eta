import { UserContext } from "../../user-context/user.context"
import { CreatePayload } from "../event.type"
export interface EventAfterCreate<T> {
    afterCreate(appUser:UserContext,payload: CreatePayload<T> ): Promise<void>
}