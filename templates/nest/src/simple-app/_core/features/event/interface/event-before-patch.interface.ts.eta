import { UserContext } from '../../user-context/user.context';
import { PatchPayload } from '../event.type';
export interface EventBeforePatch<T> {
  beforePatch(appUser: UserContext, payload: PatchPayload<T>): Promise<void>;
}
