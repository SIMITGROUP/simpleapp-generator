/**
 * This file was automatically generated by simpleapp generator. It is changable
 * --remove-this-line-to-prevent-override--
 * last change 2025-09-01
 * Author: Ks Tan
 */
import { Module } from '@nestjs/common';
import { MongooseModule } from '@nestjs/mongoose';
import { <%= it.typename%>ApiService } from './<%= camelToKebab(it.resourceName)%>-api.service';
import { <%= it.typename%>ApiController } from './<%= camelToKebab(it.resourceName)%>-api.controller';
import { <%= it.typename%>ApiResolver } from './<%= camelToKebab(it.resourceName)%>-api.resolver';

@Module({
  imports: [],
  controllers: [<%= it.typename%>ApiController],
  providers: [<%= it.typename%>ApiService, <%= it.typename%>ApiResolver],
  exports: [<%= it.typename%>ApiService, <%= it.typename%>ApiResolver],
})
export class <%= it.typename%>ApiModule {}
