/**
 * This file was automatically generated by simpleapp generator. Every
 * MODIFICATION OVERRIDE BY GENERATEOR
 * last change 2024-04-02
 * Author: Ks Tan
 */
import { Module } from '@nestjs/common';
import { MongooseModule } from '@nestjs/mongoose';
import { <%= it.typename%>Entity } from './<%= camelToKebab(it.resourceName)%>.entity';
import { <%= it.typename%>Service } from './<%= camelToKebab(it.resourceName)%>.service';
import { <%= it.typename%>Controller } from './<%= camelToKebab(it.resourceName)%>.controller';
import { <%= it.typename%>MongoSchema } from './<%= camelToKebab(it.resourceName)%>.schema';
import { <%= it.typename%>Resolver } from './<%= camelToKebab(it.resourceName)%>.resolver';

const mymodule = MongooseModule.forFeature([
      { name: '<%= it.typename%>', schema: <%= it.typename%>MongoSchema },
     ])
@Module({
  imports: [mymodule],
  controllers: [<%= it.typename%>Controller],
  providers: [<%= it.typename%>Service,<%= it.typename%>Resolver ],
  exports: [mymodule,<%= it.typename%>Service,<%= it.typename%>Resolver ],
})
export class <%= it.typename%>Module {}
