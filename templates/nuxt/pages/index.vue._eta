<template>
  <div class="p-4">
    <!-- <HeaderBar></HeaderBar> -->
    <!-- <h1 class="text text-r ight ">Welcome {{ user }}</h1> -->

    <TextPrimary class="text-xl"
      >{{ t("welcome ") }} {{ sessionUser }}!</TextPrimary
    >
    <TextSubtitle
      >Please click "Pick Company" to choose a company.</TextSubtitle
    >
  </div>
</template>
<script setup lang="ts">
/**
 * This file was automatically generated by simpleapp generator.
 * --remove-this-line-to-prevent-override--
 * last change 2025-03-16
 * author: Ks Tan
 */
const tmp = ref();
definePageMeta({
  
});

const { user, clear: clearSession, session, openInPopup } = useUserSession();
const sessionUser = computed(() => user.value?.name);
const allTenants = computed(async () => {});

onMounted(async () => {
  // tmp.value =  JSON.stringify(await $fetch('/api/profile/tenants', { method: 'get' }),null,4);
  tmp.value = (await getProfileApi().getAllTenants()).data;
});
</script>
